import { Routes } from '@angular/router';
import { LoginComponent } from '../basics/login/login.component';

export const routes: Routes = [
    { path: '', component: LoginComponent }, // Chemin par défaut pour le LoginComponent
];
